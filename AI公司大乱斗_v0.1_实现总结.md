# 《AI公司大乱斗》v0.1 实现总结

> 归档。现行游戏是 `dynamic-counter-prototype/` 里的《最后一单》。本文件只描述已冻结的办公室模拟原型。

更新日期：2026-08-10  
项目目录：`/Users/derekfly3/Documents/ChatGPT/AI公司大乱斗`

## 1. 项目定位

这是一个浏览器端的轻量职场模拟游戏原型。玩家站在公司之外观察 12 名 AI 公司员工每天发生的事情，可以查看人物状态、推进日期，并通过“上帝事件”干预下一天。

本次实现严格限定在 v0.1：先用统一的本地模拟引擎跑通完整体验，没有接入 AI API，也没有搭建 Multi-Agent 或后端系统。

## 2. 技术实现

- React + TypeScript + Vite + CSS。
- 游戏状态使用 React state 管理。
- 游戏存档使用浏览器 `localStorage`，键名为 `ai-company-brawl-v1`。
- 没有使用 Redux、Zustand、数据库、WebSocket、Worker、Docker 或 Agent Framework。
- 办公室和人物均由普通 HTML/CSS 元素绘制，没有使用 Canvas、3D 或物理引擎。

## 3. 已完成功能

### 3.1 办公室界面

- 左侧约 70% 为现代 2D 俯视办公室。
- 右侧约 30% 为公司指标、今日动态和人物卡。
- 办公室包含 7 个区域：
  - CEO 办公室
  - 会议室
  - 产品 / 研发区
  - 开放办公区
  - 茶水间
  - 销售区
  - 前台
- 家具、地板、隔断和人物均使用 CSS 绘制。
- 人物位置固定，不做寻路。
- 当人物参与当天事件时，会显示 `!`、`…`、`⚡` 或 `✓` 状态气泡。

### 3.2 固定 12 名员工

已包含以下职位：

1. CEO：沈知行
2. 技术负责人：顾言
3. 产品经理：林薇
4. 后端工程师：周岩
5. 前端工程师：陈野
6. UI 设计师：乔语
7. 销售负责人：韩峰
8. 销售：许乐
9. 运营：唐糖
10. HR：苏晴
11. 财务：赵辰
12. 实习生：陆一然

每名员工只保存以下业务字段：

- id
- 姓名
- 年龄
- 职位
- 性格
- 能力
- 野心
- 压力
- 满意度
- 离职风险
- 当前想法
- 当前任务
- 所在区域
- 少量关键人物关系

人物关系是有方向的，范围限制为 `-100 ～ +100`。例如“周岩 → 林薇”与“林薇 → 周岩”是两条独立关系。

### 3.3 人物交互

- 点击办公室里的任意人物，右侧人物卡会立即切换。
- 人物卡显示：姓名、年龄、职位、性格、能力、野心、压力、满意度、离职风险、当前任务、当前想法和关键关系。
- 人物卡直接读取当前游戏状态，因此模拟发生后所有数据会实时更新。

### 3.4 公司指标

只实现了要求中的 6 个指标：

- 公司现金
- 员工士气
- 内斗指数
- 业务压力
- 离职风险
- 老板满意度

除现金外，指标范围限制在 `0 ～ 100`。界面通过数值、进度条和风险颜色显示状态。

### 3.5 推进一天

点击顶部“推进一天”后：

1. 日期增加一天。
2. 游戏天数加一。
3. 生成 3～5 个当天事件。
4. 依次把事件效果写入人物状态、人物关系和公司指标。
5. 更新今日动态和办公室人物气泡。
6. 自动将新状态写入 `localStorage`。

事件不是纯文本故事。每个事件都带有结构化影响，并通过模拟引擎实际修改当前状态。

示例事件包括：

- 产品经理临时修改需求。
- 大客户接触竞争对手。
- 技术负责人拒绝强行上线。
- 实习生听到裁员谈话。
- 设计与前端发生冲突。
- 财务发出现金流预警。
- 销售拿下关键订单。
- 线上服务出现故障。
- HR 约压力最高的员工谈话。
- 用户反馈集中涌入。
- 产品与技术达成高效对齐。
- 竞争对手挖走离职风险最高的员工。

当天事件会在右侧显示具体变化，例如：

- `周岩 压力 +12`
- `周岩 → 林薇 -10`
- `员工士气 -3`
- `公司现金 +46万`

### 3.6 上帝事件

- 玩家在顶部输入事件并点击“上帝事件”后，文本会保存为 `pendingGodEvent`。
- 界面显示“已排入下一天”的提示，可在执行前取消。
- 下一次推进时，上帝事件会作为当天 3～5 个事件之一进入模拟。
- 执行后自动清空 `pendingGodEvent`。

当前包含一些关键词规则：

- 裁员 / 优化
- 取消年终奖 / 不发奖金
- 加薪 / 奖金 / 分红
- 融资 / 投资到账
- 客户 / 订单 / 签约 / 回款
- 团建 / 放假 / 休假
- 其他任意事件使用通用影响规则

例如输入“CEO 宣布今年没有年终奖”后，会真实产生以下影响：

- 员工士气下降。
- 大部分员工压力和离职风险上升。
- 高野心或低满意度员工的离职风险增加更多。
- 部分员工对 CEO 的关系下降。
- 公司现金、内斗指数和老板满意度发生对应变化。

### 3.7 存档与重新开始

- 每次游戏状态变化后自动保存到 `localStorage`。
- 刷新网页后，日期、公司指标、人物状态、人物关系和当天事件仍然保留。
- 点击“重新开始”会恢复第 1 天、初始人物数据、初始关系和初始公司指标。

## 4. 状态与模拟流程

核心游戏状态为：

```text
GameState
├── day
├── date
├── company（6 项公司指标）
├── employees（12 名员工）
├── todayEvents（当天 3～5 个事件）
└── pendingGodEvent（待执行上帝事件）
```

推进一天的主要流程：

```text
当前 GameState
    ↓
日期 +1，决定事件数量 3～5
    ↓
尽量避开与上一天标题重复的随机事件
    ↓
如有 pendingGodEvent，则加入当天事件
    ↓
依次应用人物变化、关系变化、公司指标变化
    ↓
生成新的 GameState 并清空 pendingGodEvent
    ↓
React 重新渲染 + localStorage 保存
```

所有人物数值、公司百分比指标和关系值都会在写入时执行边界限制，避免越界。

## 5. 核心文件

- `src/App.tsx`
  - 主界面和全部交互。
  - React 状态、localStorage、办公室、人物卡、公司指标和事件列表。
- `src/data.ts`
  - 12 名员工初始数据。
  - 初始公司指标、办公区域名称和人物固定位置。
- `src/simulation.ts`
  - 统一模拟引擎。
  - 普通事件模板、上帝事件关键词规则、日期推进、数值限制和状态写入。
- `src/types.ts`
  - 员工、关系、公司指标、事件和游戏状态的 TypeScript 类型。
- `src/styles.css`
  - 完整办公室、人物、顶部操作区、右侧面板和响应式样式。
- `src/main.tsx`
  - React 入口。
- `package.json`
  - Vite 开发、TypeScript 构建和依赖配置。

## 6. 启动方式

```bash
cd "/Users/derekfly3/Documents/ChatGPT/AI公司大乱斗"
npm install
npm run dev
```

默认访问地址：

```text
http://127.0.0.1:5173/
```

生产构建检查：

```bash
npm run build
```

## 7. 已完成验证

已实际启动项目并在浏览器中完成以下检查：

- 页面能正常打开。
- 能看到 7 个办公区域和全部 12 名员工。
- 点击后端工程师后，人物卡正确切换到周岩。
- 推进一天后日期从第 1 天变为第 2 天。
- 单日成功生成过 5 个普通事件。
- 人物压力、满意度、离职风险和当前任务真实变化。
- “周岩 → 林薇”的关系从 `-18` 真实变化到 `-28`。
- 6 项公司指标发生真实变化。
- 输入“CEO宣布今年没有年终奖。”后，下一天成功生成对应上帝事件。
- 上帝事件执行后 `pendingGodEvent` 自动清空。
- 刷新网页后日期、指标和当天事件保持不变。
- 点击“重新开始”后恢复第 1 天、0 个事件和全部初始指标。
- `npm run build` 通过。
- 浏览器控制台没有发现 error 或 warning。

## 8. 当前明确没有实现的内容

以下内容是按 v0.1 范围主动不做，并非遗漏：

- AI API 或大模型动态生成事件。
- 12 个独立 Agent 或 Multi-Agent 系统。
- 人物寻路、物理碰撞和 Canvas 游戏引擎。
- 复杂人物动画或复杂美术资产。
- 数据库、Redis、消息队列、Worker、WebSocket。
- 用户登录、权限、后台管理和多人功能。
- Redux、Zustand、插件系统或复杂架构抽象。

## 9. 供其他模型审阅的重点

建议审阅时重点检查以下内容：

1. `simulation.ts` 中所有事件是否都能产生有意义且方向合理的状态变化。
2. 随机事件在连续运行很多天后，数值边界与游戏平衡是否仍然合理。
3. 上帝事件关键词匹配是否存在语义误判。
4. `localStorage` 中旧版本或异常数据的兼容处理是否足够。
5. 1280×720 及更小屏幕下，右侧滚动和办公室人物标签是否保持可用。
6. 事件影响摘要是否足以让玩家理解“发生了什么”和“为什么数值变了”。

